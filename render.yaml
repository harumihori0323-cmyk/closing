services:
  - type: web
    name: closing-support
    runtime: node
    region: singapore
    plan: free
    nodeVersion: "20"
    buildCommand: cd frontend && npm ci && npm run build && cd ../backend && npm ci
    startCommand: cd backend && node server.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: ANTHROPIC_API_KEY
        sync: false
      - key: CORS_ORIGINS
        value: https://cl.milkgodo.com
